# Screenshots Do PR 2

Capturas do PR `feat/design-system-base`, relacionado à issue [#124](https://github.com/lspassos1/aframe-kingspan-local/issues/124).

Viewports:

- Desktop: `1440x1000`
- Mobile: `390x844`

Sessão:

- Dev server local em `http://localhost:3000`.
- Sessão Clerk autenticada real via ticket de sign-in do usuário de auditoria.
- Projeto exemplo carregado por `/start?mode=example` antes das rotas internas.
- Rotas internas capturadas por navegação no shell autenticado, não por reload direto.

Rotas capturadas:

- `/`
- `/start`
- `/dashboard`
- `/edit`
- `/budget`
- `/budget-assistant`
- `/model-3d`
- `/export`
- `/help`

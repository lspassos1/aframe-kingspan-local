# Screenshots Do PR 3

Capturas do PR `feat/home-design-reset`, relacionado à issue [#125](https://github.com/lspassos1/aframe-kingspan-local/issues/125).

Viewports:

- Desktop: `1440x1000`
- Mobile: `390x844`

Sessão:

- Dev server local em `http://localhost:3000`.
- Home pública em estado sem sessão autenticada.
- Browser local validou a rota e os textos principais.
- PNGs gerados por Playwright local após a validação no Browser, porque a captura visual direta do Browser travou em `Page.captureScreenshot`.

Rotas capturadas:

- `/`
